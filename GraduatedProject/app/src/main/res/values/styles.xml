<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="girisBtn" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@android:color/holo_red_dark</item>
        <item name="colorButtonNormal">@android:color/holo_green_dark</item>
    </style>

    <style name="kayitBtn" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@android:color/holo_green_dark</item>
        <item name="colorButtonNormal">@android:color/holo_red_dark</item>
    </style>
    <style name="registerBtn" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@android:color/holo_blue_bright</item>
        <item name="colorButtonNormal">@android:color/holo_orange_light</item>
    </style>

</resources>
